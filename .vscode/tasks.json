{
    "tasks": [
        {
            "type": "catkin_make_isolated",
            "args": [
                "--directory",
                "${workspaceFolder}/../..",
                "--pkg",
                "nc_runtime"
            ],
            "problemMatcher": [
                "$catkin-gcc"
            ],
            "group": "build",
            "label": "catkin_make: build"
        }
    ]
}